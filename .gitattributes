# Normalize to LF in the repo and in working trees. Prettier is configured with
# endOfLine: "lf", so without this `pnpm lint` fails on Windows checkouts.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
pnpm-lock.yaml -diff
