# pnpm 11 gates packages published within the last 24h and, when it needs to
# record an exception, writes a pnpm-workspace.yaml. This project is deliberately
# NOT a pnpm workspace, so the gate is disabled here instead.
minimum-release-age=0
